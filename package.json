{
  "name": "wp-dockerize",
  "version": "1.0.0",
  "description": "WP Project + Docker for development",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "docker-compose -f docker-compose.yml -f docker-compose.http.yml up -d",
    "ssl": "docker-compose -f docker-compose.yml -f docker-compose.https.yml up -d",
    "stop": "docker-compose -f docker-compose.yml -f docker-compose.https.yml down"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Y2-MosheHarush/wp-dockerize.git"
  },
  "author": "Moshe Harush",
  "license": "GPL 3"
}
